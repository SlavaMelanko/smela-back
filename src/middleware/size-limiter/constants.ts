export const DEFAULT_MAX_SIZE = 100 * 1024 // 100KB default
export const AUTH_ROUTES_MAX_SIZE = 10 * 1024 // 10KB for auth routes
export const FILE_UPLOAD_MAX_SIZE = 5 * 1024 * 1024 // 5MB for file uploads
export const DEFAULT_STREAMING_THRESHOLD = 100 * 1024 // 100KB threshold for streaming
